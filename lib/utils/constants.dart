import 'package:flutter/material.dart';

const Color primaryColor = Color.fromRGBO(68, 87, 114, 1.0);
const Color dropdownColor = Color.fromRGBO(60, 77, 101, 1.0);
const Color textColor = Color.fromRGBO(255, 249, 224, 1.0);
const Color dialogColor = Color.fromRGBO(29, 52, 53, 1.0);

ThemeData themeData = ThemeData(
  primaryColor: primaryColor,
  toggleableActiveColor: primaryColor,
  visualDensity: VisualDensity.adaptivePlatformDensity,
);
